"""Projects module — owner Project.

Public API:
  - app.shared.schemas.ProjectRead — публичная схема (в общей зоне)
  - app.projects.service.get_project_by_id — read через сервис

Internal:
  - app.projects.models.Project — SQLAlchemy-модель (приватная)
"""
