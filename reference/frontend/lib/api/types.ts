/**
 * Сгенерируется автоматически из OpenAPI после /contracts.
 *
 * Команда регенерации (из корня проекта):
 *   ./scripts/gen-types.sh
 *
 * НЕ редактировать вручную — изменения будут перетёрты.
 */

export type Placeholder = unknown;
